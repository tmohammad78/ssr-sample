import React from "react"
function Home() {
    return (
        <div>
            Home page
            <button onClick={() => alert("See Alert")}>click to see alert</button>
        </div>
    )
}

export default Home;